spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/asset_registry
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
  flyway:
    enabled: true
server:
  port: 8080

springdoc:
  swagger-ui.enabled: true
  api-docs.enabled: true
